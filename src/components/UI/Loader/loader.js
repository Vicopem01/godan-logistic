import classes from "./loader.module.css";

const Loader = ({ color }) => {
  const style = {
    border: `7px solid ${color}`,
    borderTopColor: "transparent",
  };
  return (
    <div className={classes.loadingiospinnerrollingfhkdpoa82e}>
      <div className={classes.ldiopboagp1d3eb}>
        <div style={style}></div>
      </div>
    </div>
  );
};

export default Loader;
